## Model documentation

When a Pydantic model or dataclass serves as a config or data contract (consumed by tools, stored as JSON, etc.), document fields with comments in the code — either inline (`field: type  # description`) for short descriptions, or a comment line above the field for longer ones. This is the canonical reference that docs should point to.
